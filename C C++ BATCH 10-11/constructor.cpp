A Constructor in c++ is special memeber function(method) of
class that initializes objects of the class.
It has the same name as the class and does not have return
type.
Constructors are called automatically when an object of the class created.

Types of constructor:
    Default consturctor:
        A constructor that takes no arguments

    Parameterized Constructor:
        A constructor that takes arguments to initilize the
        object with specific values.

    Copy Constructor:
        A Constructor that initilizes an objecct using another
        object of the same class.
